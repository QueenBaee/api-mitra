npm install
node server.js

url path enable disable

{url}/disable={ID Customer}
{url}/enable={ID Customer}

url path tambah pelanggan baru

{url}/add={ID Customer}?add={IP Public}

url path tambah pelanggan baru dengan lebih dari 1 IP Public

{url}/add={ID Customer}?add={IP Public},{IP Public}
